# Dummy example (add 0 or 0*100)
province = {
	war = {
		active = {
			is_at_war = yes
		}
		eval_add = {
			factor = 100
			modifier = {
				factor = 100
				all_neighbor_province = {
					NOT = {
						controlled_by = ROOT
					}
				}
			}
		}
	}
}