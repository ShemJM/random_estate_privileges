# Dummy example (multiply by 1)
province = {
    peace = {
        active = {
            is_at_war = no
        }
        eval_multiply = {
            factor = 1
			modifier = {
				factor = 10
				has_terrain = forest
			}
        }
    }
}
